using UnityEngine;

public class MusicPersist : MonoBehaviour
{
    private static MusicPersist _instance;

    private void Awake()
    {
        if (_instance == null)
        {
            _instance = this;
            DontDestroyOnLoad(gameObject);
        }
        else
        {
            Destroy(gameObject);
        }
    }
}